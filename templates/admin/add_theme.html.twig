{% extends 'base.html.twig' %}

{% block body %}
<h1>Ajouter un Thème</h1>

{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
    {{ form_row(form.image) }}
    {{ form_row(form.save) }}
{{ form_end(form) }}

<a href="{{ path('app_admin_content') }}">Retour</a>
{% endblock %}

